{
  "name": "ieec-ya-connect",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @ieec/web",
    "build": "npm run build:web",
    "build:web": "npm run build -w @ieec/web",
    "dev:mobile": "npm run start -w @ieec/mobile",
    "build:shared": "npm run build -w @ieec/shared",
    "typecheck": "npm run typecheck -w @ieec/shared && npm run typecheck -w @ieec/web",
    "emulators": "npx -y firebase-tools@14.11.0 emulators:start --only auth,firestore --project ieec-ya-connect-a1ae1",
    "seed:emulator": "npm install --prefix scripts && node scripts/seed-firebase-emulator.mjs",
    "seed:bootstrap": "npm install --prefix scripts && node scripts/bootstrap-firebase-project.mjs",
    "firebase:deploy": "npx -y firebase-tools@14.11.0 deploy --only firestore",
    "firebase:deploy:rules": "npx -y firebase-tools@14.11.0 deploy --only firestore:rules",
    "firebase:login": "npx -y firebase-tools@14.11.0 login"
  },
  "engines": {
    "node": ">=20"
  }
}
